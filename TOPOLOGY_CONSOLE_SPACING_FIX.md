# Topology and Console Spacing Fix

The right-side topology controls now use a fixed 540-pixel scrollable panel,
matching the full rendered topology card height. Selecting **Connect** no longer
expands the entire topology row or pushes the Console farther down the page.

The connector color preview was also reduced to a compact height while keeping
the Ethernet, fiber, serial, and wireless line samples visible.
