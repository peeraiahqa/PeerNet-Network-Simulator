# PeerNet Network Simulator
# These can point to the SAME Supabase project used by PeerNet AI if you
# want the same user accounts/login credentials to work in both apps.
#
# The Simulator stores its app data only in public.simulator_projects.
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key
